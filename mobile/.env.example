# Backend base URL. In dev, point at your machine's LAN IP so a physical
# Android device can reach it (localhost on the phone is the phone itself).
# Example: EXPO_PUBLIC_BACKEND_URL=http://192.168.1.5:4000
EXPO_PUBLIC_BACKEND_URL=http://localhost:4000
